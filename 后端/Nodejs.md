
让js脱离了浏览器，能在电脑或服务器上直接跑。Node.js 把 JS 引擎(V8)单独拎出来,让 JS 能干**电脑能干的活**——读文件、写文件、监听网络端口、起服务器。

| |浏览器里的 JS|Node.js|
|---|---|---|
|在哪跑|浏览器引擎里|电脑/服务器上直接跑|
|能干啥|操作网页 DOM、发请求|读文件、起服务器、跑工具|
|有没有 `document`、`window`|有|**没有**|

































































